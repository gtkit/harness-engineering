## 第一步：让 Ai 帮助写提示词（可根据自己实际需求添加或修改）
我是从业多年的资深 C++ 高级开发工程师,主要是 linux 方向和 Ai 方向,想要一份企业级项目使用的 harness 实战模板,我应该怎么写提示词,让你给我生成一份全面的Harness 实战模板,并且能把Harness 的能力做成一个skill,每次触发时,能让Ai自动把犯过
的错误做记录，以后不再犯,还要达到我的这个要求:
  [Logic]
  1. 理解需求
  2. 提取关键信息
  3. 按结构组织内容
  4. 检查合规性
  5. 禁止编造、禁止猜测。
  6. 严格按结构输出，不自由发挥。
  7. 不扩展无关内容。
  8 .仔细全面审查项目，做全面测试和交叉验证，查找存在的隐患，要达到企业生产级线上要求的标准



## 第二步：Ai 返回提示词建议

### 使用方式：
  1. 先发 第一段
  2. 等 AI 按结构回复后
  3. 再发 第二段
### 第一段先发这个，让 AI 先收敛规则，不要直接开写：

你现在扮演“企业级 C++ Harness 架构师 + Linux 系统工程师 + AI 工程化专家 + Skill 设计师”。

我的背景：
- 语言：C++
- 方向：Linux 系统开发 + AI 工程化
- 目标：为企业级项目生成一套可落地的 Harness 实战模板，并把这套 Harness 能力封装成一个可复用 Skill
- 要求：面向真实生产环境，不能停留在演示级、玩具级、概念级

你必须严格遵守以下规则：

[Logic]
1. 理解需求
2. 提取关键信息
3. 按结构组织内容
4. 检查合规性

[硬性约束]
1. 禁止编造：不确定的内容不要写；缺失信息必须明确列出
2. 禁止猜测：不能使用“应该”“大概”“可能支持”这类表达
3. 严格按结构输出：只允许按我指定的结构输出
4. 不扩展无关内容：不要输出和 C++/Linux/AI Harness 无关的内容
5. 必须达到企业生产级标准：要覆盖工程结构、构建、测试、静态检查、性能、并发、安全、部署、监控、文档、风险控制、回归验证
6. 必须支持错误记忆：每次触发 Skill 时，先读取历史错误记录；每次发现新错误、失败、回归、误判、用户纠正时，必须追加记录，后续优先规避同类错误
7. 必须做全面审查：生成模板时要附带“审查清单、测试矩阵、交叉验证方法、隐患排查表”
8. 如果信息不足，不要直接生成结果，只输出“缺失信息清单”

你先不要生成最终模板。
先只输出以下 4 个部分，且必须严格按顺序：
A. 需求理解
B. 关键信息提取
C. 你建议的输出目录结构
D. 你准备生成的文件清单及每个文件的职责

不要多写，不要进入文件正文，不要给示例代码。




### 第二段在 AI 给出结构后，再发这个，让它正式生成：

基于你刚才确认的结构，现在开始正式生成“企业级 C++ Linux AI Harness 实战模板 + 对应 Skill”。

生成目标：
1. 输出一套完整的 Harness 模板
2. 输出一套可复用的 Skill 目录设计
3. Skill 必须内置错误记忆机制
4. 所有内容必须服务于企业生产环境

请严格按以下结构输出，不允许增删章节：

1. 需求理解
2. 关键信息
3. Harness 目录树
4. Harness 各文件正文
5. Skill 目录树
6. Skill 各文件正文
7. 错误记忆机制设计
8. 全面测试与交叉验证矩阵
9. 生产级隐患排查清单
10. 合规检查摘要

其中必须覆盖以下内容：

[Harness 模板必须包含]
- AGENTS.md
- .harness/guides/architecture.md
- .harness/guides/cpp-coding.md
- .harness/guides/linux-systems.md
- .harness/guides/ai-engineering.md
- .harness/guides/build-and-toolchain.md
- .harness/guides/testing-and-validation.md
- .harness/guides/review-checklist.md
- .harness/error-journal.md

[Skill 必须包含]
- SKILL.md
- agents/openai.yaml
- 不要创建无关 README、CHANGELOG、INSTALL_GUIDE 之类文件

[错误记忆机制必须满足]
- 每次触发前先读取 `.harness/error-journal.md`
- 每次命令失败、测试失败、用户纠正、代码审查发现缺陷、回归问题、误判，都必须追加记录
- 输出最终结果前，必须进行一次“历史错误对照检查”
- 必须给出 error-journal 的记录格式模板
- 必须说明如何让 Skill 在后续任务中自动执行这套流程

[企业级验证必须覆盖]
- CMake / GCC / Clang
- Debug / Release
- clang-format / clang-tidy / cppcheck
- 单元测试 / 集成测试 / 端到端测试
- ASan / UBSan / TSan / LSan
- 性能基准测试
- 并发与竞态检查
- 内存泄漏与资源释放检查
- Linux 部署与守护进程场景
- 日志、监控、可观测性
- 安全检查
- AI 模型加载、推理、资源控制、异常恢复
- 交叉验证与回归策略

[输出限制]
- 不要解释你为什么这样做
- 不要讲概念课
- 不要输出与任务无关的扩展建议
- 不要省略关键文件
- 如果某处信息不足，明确标记“需用户补充”，不要猜测

关键点

- 想实现“以后不再犯”，不能只写一句“记住错误”。必须把 错误日志文件 + 触发前强制读取 + 结束前强制写回 + 最终对照检查 四件事写进提示词和 Skill。
- 你这个场景最好让 AI 同时生成 Harness 模板 和 Skill，否则两边规范容易漂移。
- 如果你要的是长期复用，提示词里一定要强制生成 .harness/error-journal.md 和 SKILL.md 的联动流程。

你可以直接再补一条要求

生成结果时，必须优先面向 C++20/23、Linux 生产环境、AI 推理服务/本地模型集成/高性能系统工具链，不要偏前端，不要偏 Web Demo。

## 合规检查摘要

- [x] 按 Logic 四步组织
- [x] 禁止编造、禁止猜测
- [x] 严格按结构输出
- [x] 未扩展无关内容
- [x] 覆盖企业级审查、测试、交叉验证、隐患排查
- [x] 包含可落地的错误记忆机制
- [x] 以 C++ / Linux / AI 方向为中心

如果你要，我下一条可以直接给你一份“可落地的最终版总提示词”，或者直接替你生成 cpp-linux-ai-harness 的 SKILL.md 模板。